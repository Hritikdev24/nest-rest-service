import { AllResponseInterceptor } from './all-response.interceptor';

describe('AllResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new AllResponseInterceptor()).toBeDefined();
  });
});
